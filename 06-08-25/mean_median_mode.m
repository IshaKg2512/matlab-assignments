a = [5, 2, 9, 2, 7, 5, 10];

mean_val = mean(a);
median_val = median(a);
mode_val = mode(a);

fprintf('Mean: %.2f\n', mean_val);
fprintf('Median: %.2f\n', median_val);
fprintf('Mode: %.2f\n', mode_val);
